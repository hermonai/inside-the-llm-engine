ENGINE-1 STOCHASTIC SAMPLING PIPELINE

solid arrow = derived data                 raw Logits remain unchanged

  MODEL FORWARD
        │
        ▼
  FINITE RAW LOGITS [V_vocab] f32
        │
        ▼ copy to request-local f64 workspace
  ┌───────────────────┐
  │ divide by T, T>0  │
  └───────────────────┘
            ▼
  ┌───────────────────┐
  │ top-k logit mask  │  None = disabled; k >= V = no change
  └───────────────────┘
            ▼
  ┌───────────────────┐
  │ stable softmax    │  subtract maximum before exponentiation
  └───────────────────┘
            ▼
  ┌───────────────────┐
  │ top-p probability │  include threshold-crossing candidate
  │ filter            │
  └───────────────────┘
            ▼
  ┌───────────────────┐
  │ renormalize       │  finite, non-negative, sum approximately 1
  └───────────────────┘
            ▼
  ┌───────────────────┐      request-owned SplitMix64 state
  │ categorical draw  │ ◀──────────────────────────────────┐
  └───────────────────┘                                    │
            ▼                                              │
         TokenId                               seed ─▶ mutable RNG

Greedy is a separate path: raw logits ─▶ one max scan ─▶ lowest ID wins ties.
