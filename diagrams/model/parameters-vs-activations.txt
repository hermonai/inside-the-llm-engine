PARAMETERS, ACTIVATIONS, AND OWNERS

                         MODEL LIFETIME
              immutable parameters shared by requests
        ┌───────────────────────────────────────────────┐
        │ E [V_vocab,D]  W [V_vocab,D]  b [V_vocab]    │
        └───────────────────────┬───────────────────────┘
                                │
                   ┌────────────┴────────────┐
                   │                         │
                   ▼                         ▼
        Request A / forward A      Request B / forward B
        ┌────────────────────┐     ┌────────────────────┐
        │ input x_A          │     │ input x_B          │
        │ [hidden h_A]       │     │ [hidden h_B]       │
        │ [logits z_A]       │     │ [logits z_B]       │
        └────────────────────┘     └────────────────────┘

Legend: (none) immutable model data    [X] request-owned mutable activation

Requests may share parameters; they must not share mutable activations.
