# Repository Scripts

Phase 0 repository checks live here. Language-specific format, test, diagram,
and link checks will be added with the first executable artifacts rather than
introducing a toolchain before content exists.
